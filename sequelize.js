/*
-> linking an object-relational Mapper(ORM) to a node.js 
application can be challenging because there arent any guides or documentation available
-> sequelize orm is like a helper tool for programmers creating applications that needs databases
-> its like a friendly bridge connecting our code with in node.js with the database where we store all
the essential information

// sequelization 

-> it means linking a node.js application to a special tool that helps
database and the app work better together
-> this tool is called an object-relational mapper and its used to make sure 
the database and the app are in sync


// ORM

-> orm stands for object relational mapper, is like a helper that lets us work with data in a database using objects
-> 

// benefits

-> easier data handling : easy to create, retrieve, update and delete data in our database
-> database agnostic : sequelize works with different types of databases like mysql, postgresql, ms sql
this means we can switch databases without rewriting a lot of code
-> object oriented approach
-> data validation : it helps us ensure that the data we insert into the database follows certain rules
-> Migrations : when our app evolves and our datbase structure changes sequelize helps manage these changes
smoothly without causing data loss
-> security : sequelize helps guard against sql injections attacks, a common type of security vulnerability 
that can occur when using raw sql queries


*/